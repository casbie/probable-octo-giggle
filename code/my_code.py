def add(first, second):
    """Add two number
    
    Args:
        first (float): the first number
        second (float): the second number
    
    Returns:
        float: the summation of the two input numbers
    """
    result = first + second
    return result